# GitHub Pages 更新说明

固定访问地址：

`https://joyceleo326.github.io/JianKangShouHuZhe/`

二维码只需要生成一次。后续更新 App 后，把代码推送到 GitHub 的 `main` 分支，GitHub Actions 会自动重新构建并发布到同一个网址。

本地手动构建命令：

```bash
npm run build:pages
```

构建产物目录：

`release/github-pages-deploy`
